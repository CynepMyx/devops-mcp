# Pinned to what the running container actually has, not to whatever is newest.
# The image is rebuilt rarely, so an unpinned bump lands as a surprise during a
# deploy — paramiko in particular is what every SSH and SFTP tool runs on.
# To bump: change a line, deploy.sh, then check `docker exec devops-mcp pip freeze`.
mcp==1.27.0
fastapi==0.136.1
uvicorn[standard]==0.46.0
psutil==7.2.2
docker==7.1.0
anyio==4.13.0
dbus-fast==4.0.4
watchdog==6.0.0
paramiko==4.0.0
httpx==0.28.1
asyncpg==0.31.0
aiomysql==0.3.2
pytest==9.0.3
